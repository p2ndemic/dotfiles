Вот хороший стартовый вариант настройки **kanshi** под твой 16-дюймовый ноутбук с экраном 2560×1600 и Labwc на Arch Linux.

### 1. Сначала узнай точное имя встроенного дисплея

Выполни в терминале (когда подключён только ноутбук):

```bash
kanshi -d    # самый удобный способ
# или
wlr-randr
# или
kscreen-doctor -o    # если установлен plasma-wayland-session
```

Очень часто на 16" ноутбуках (особенно 2022–2025 годов) это будет один из вариантов:

- `eDP-1`
- `eDP-2`
- `eDP-1-1`

Запомни/запиши это имя. Дальше будем использовать `eDP-1` как пример.

### 2. Рекомендуемая конфигурация ~/.config/kanshi/config

```bash
# ------------------------------------------------------
# Профиль 1 — только встроенный экран ноутбука (самый важный)
# ------------------------------------------------------
profile laptop-only {
    output eDP-1 {
        mode 2560x1600@60Hz     # можно попробовать 165Hz/144Hz, если твой экран поддерживает
        scale 2
        position 0,0
    }
}

# ------------------------------------------------------
# Профиль 2 — ноутбук + внешний монитор справа (самый популярный вариант)
# ------------------------------------------------------
profile laptop-external-right {
    output eDP-1 {
        mode 2560x1600@60Hz
        scale 2
        position 0,0
    }

    output HDMI-A-1 {                   # ← поменяй на своё имя, если другое
        mode 1920x1080@60Hz             # ← подставь нужное разрешение/частоту
        scale 1.75                      # ← очень часто 1.75–2.0 комфортнее всего
        position 2560,0                 # ← внешний монитор справа от встроенного
    }
}

# ------------------------------------------------------
# Профиль 3 — только внешний монитор (крышка закрыта)
# ------------------------------------------------------
profile external-only {
    output eDP-1 disable

    output HDMI-A-1 {
        mode 3840x2160@60Hz           # ← пример для 4K, поменяй под свой
        scale 1.5                     # ← 1.5–2.0 в зависимости от размера и предпочтений
        position 0,0
    }
}

# ------------------------------------------------------
# Запасной профиль по умолчанию (если ничего не подошло)
# ------------------------------------------------------
profile fallback {
    output eDP-1 {
        mode 2560x1600@60Hz
        scale 2
    }
}
```

### 3. Самые популярные варианты scale для разных ситуаций

Экран ноутбука 2560×1600 (16")        | Комфортный scale | Примечание
---------------------------------------|------------------|-----------------------
Только встроенный экран                | `2` или `2.0`    | Самый чёткий вариант
Встроенный + 27–32" 4K                 | 1.75–2.0 / 1.5–1.75 | Часто 1.75 выглядит лучше всего
Встроенный + 24–27" 1440p              | 1.5–1.8          | —
Только большой 4K 27–32"               | 1.5–1.75         | Самый популярный выбор
Только большой 4K 27–43"               | 1.25–1.5         | Если хочется очень крупный интерфейс

### 4. Как запускать (рекомендуется)

Самый надёжный и популярный способ — через `autostart` в Labwc:

```bash
# ~/.config/labwc/autostart

kanshi >/dev/null 2>&1 &
```

Либо через systemd user unit (тоже отлично работает):

```bash
# ~/.config/systemd/user/kanshi.service
[Unit]
Description=Kanshi dynamic display configuration
PartOf=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/bin/kanshi
Restart=on-failure

[Install]
WantedBy=graphical-session.target
```

```bash
systemctl --user enable --now kanshi.service
```

### 5. Быстрые команды для тестирования

```bash
# Посмотреть, какое сейчас имя и статус
kanshi -d

# Перезагрузить конфиг без перезапуска
pkill -HUP kanshi

# Принудительно перезапустить
pkill kanshi && kanshi &
```

Удачи с настройкой!  
Если скажешь точные имена портов (`wlr-randr`) и какое разрешение/размер внешнего монитора — подскажу ещё более точный вариант. 😊



Вот оптимальный и надёжный конфиг для твоей ситуации на основе Arch Wiki, мануала и особенностей Labwc.

Ты используешь ноутбук с HiDPI-экраном 2560×1600 (нужен scale 2) и иногда подключаешь один внешний монитор. Цель — автоматически переключаться между двумя сценариями:

1. Только ноутбук (внешний отключён).
2. Ноутбук + внешний монитор (обычно справа).

### Шаг 1: Узнай точные идентификаторы дисплеев
Выполни команду (установи, если нет: `sudo pacman -S wlr-randr` или `hyprctl`, если ставил Hyprland раньше):

```bash
wlr-randr --verbose
```

Пример вывода для встроенного экрана:
```
eDP-1 "BOE 0x0AB1 Unknown" enabled
  Current mode: 2560x1600@60.013Hz
  Make: BOE
  Model: 0x0AB1
  Serial: (пусто → Unknown)
```

Запиши полное описание встроенного экрана в формате `"Make Model Serial"` (если serial пустой — пиши `Unknown`).

Когда подключишь внешний монитор — повтори и запиши его описание (например, `"Dell Inc. DELL U2723QE ABC12345"`).

### Шаг 2: Рекомендуемый конфиг `~/.config/kanshi/config`
Этот вариант использует подход из Arch Wiki с **дефолтами и алиасами** — самый удобный и читаемый.

```
# Дефолты для встроенного экрана ноутбука (замени на своё реальное описание!)
output "BOE 0x0AB1 Unknown" {
    mode 2560x1600@60Hz
    position 0,0
    scale 2
    alias $laptop
}

# Дефолты для внешнего монитора (опционально, если он всегда один и тот же)
# Если внешний меняется или неизвестен заранее — пропусти этот блок и используй * ниже
output "Dell Inc. DELL U2723QE *" {  # Wildcard для serial, если он меняется
    mode --preferred            # Лучший режим автоматически
    position 1280,0             # Справа от ноутбука (логическая ширина 2560/2 = 1280)
    scale 1                     # Измени на 2, если внешний тоже HiDPI
    alias $external
}

# Профиль: только ноутбук
profile laptop-only {
    output $laptop enable
    output * disable            # Выключаем любые другие outputs
}

# Профиль: ноутбук + внешний монитор
profile docked {
    output $laptop enable
    output $external enable     # Если задал алиас выше
    # Или, если внешний монитор разный/неизвестен заранее:
    # output * {
    #     enable
    #     mode --preferred
    #     position 1280,0
    #     scale 1
    # }
}
```

**Почему этот конфиг хороший:**
- Дефолты задаются один раз — легко менять параметры.
- Алиасы (`$laptop`, `$external`) делают профили читаемыми.
- `output * disable` в профиле laptop-only гарантирует, что при отключении внешнего экрана он точно выключится.
- Позиция `1280,0` правильная: при scale 2 логическое разрешение ноутбука — 1280×800.
- `--preferred` удобно для внешнего — берёт максимальный поддерживаемый режим.

Если внешний монитор всегда разный (например, дома один, на работе другой) — удали блок с `$external` и используй вариант с `output * { ... }` внутри профиля `docked`.

### Шаг 3: Автозапуск (рекомендация из Arch Wiki)
Создай user-сервис systemd (самый надёжный способ):

```bash
mkdir -p ~/.config/systemd/user
cat > ~/.config/systemd/user/kanshi.service << EOF
[Unit]
Description=Kanshi dynamic display configuration
Documentation=https://gitlab.freedesktop.org/emersion/kanshi

[Service]
Type=simple
ExecStart=/usr/bin/kanshi -c %h/.config/kanshi/config
Restart=always
RestartSec=3

[Install]
WantedBy=graphical-session.target
EOF
```

Затем:

```bash
systemctl --user daemon-reload
systemctl --user enable --now kanshi.service
```

### Шаг 4: Проверка
- Запусти `kanshi` вручную в терминале и подключай/отключай монитор — в выводе увидишь, какой профиль применяется.
- Логи: `journalctl --user -u kanshi -f`

Если пришлёшь точный вывод `wlr-randr --verbose` (особенно строки с make/model/serial для встроенного экрана), я подставлю точные значения в конфиг. Удачи — после правильной настройки всё будет переключаться автоматически и без глюков!
