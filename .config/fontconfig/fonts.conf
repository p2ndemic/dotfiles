<?xml version='1.0'?>
<!DOCTYPE fontconfig SYSTEM 'fonts.dtd'>
<fontconfig>

 <!-- ════════════════════════
      RENDERING
      ════════════════════════ -->

 <!-- Сглаживание контуров глифов. Отключать смысла нет даже на
      HiDPI — grayscale-антиалиасинг работает чисто и без
      артефактов при любой плотности пикселей.
      Default: true -->
 <edit mode="assign" name="antialias">
  <bool>true</bool>
 </edit>

 <!-- Хинтинг подгоняет контуры глифов к пиксельной сетке.
      При Scale 2 (189 PPI физических) пикселей достаточно для
      точной передачи формы — хинтинг только искажает геометрию
      без какой-либо видимой пользы. Отключаем.
      Default: true -->
 <edit mode="assign" name="hinting">
  <bool>false</bool>
 </edit>

 <!-- Субпиксельный рендеринг (R-G-B) утраивал горизонтальное
      разрешение на экранах с низким DPI. На HiDPI с Scale 2
      субпиксели слишком мелкие — этот режим даёт цветовые
      артефакты без прироста чёткости. Используем чистый
      grayscale-антиалиасинг.
      Default: rgb -->
 <edit mode="assign" name="rgba">
  <const>none</const>
 </edit>

 <!-- LCD-фильтр подавляет цветовые бахромы при субпиксельном
      рендеринге. Поскольку субпиксельный рендеринг отключён
      (rgba=none выше), фильтр не нужен.
      Default: lcddefault -->
 <edit mode="assign" name="lcdfilter">
  <const>lcdnone</const>
 </edit>

 <!-- ════════════════════════
      EMBEDDED BITMAPS
      ════════════════════════ -->

 <!-- Встроенные растровые глифы (bitmap strikes) — пережиток
      экранов с низким DPI. На HiDPI они отображаются размытыми
      или ступенчатыми, поэтому отключаем принудительно.
      Default: true -->
 <edit mode="assign" name="embeddedbitmap">
  <bool>false</bool>
 </edit>

 <!-- Noto Color Emoji — чисто растровый шрифт: он целиком
      состоит из embedded bitmap и без них не работает вовсе.
      Восстанавливаем исключение поверх глобального запрета. -->
 <match target="font">
  <test name="family" qual="any">
   <string>Noto Color Emoji</string>
  </test>
  <edit mode="assign" name="embeddedbitmap">
   <bool>true</bool>
  </edit>
 </match>

 <!-- ════════════════════════
      FONT ALIASES
      ════════════════════════ -->

 <!--
 <alias>
  <family>sans</family>
  <prefer>
   <family>IBM Plex Sans</family>
   <family>Noto Color Emoji</family>
  </prefer>
 </alias>

 <alias>
  <family>serif</family>
  <prefer>
   <family>IBM Plex Sans</family>
   <family>Noto Color Emoji</family>
  </prefer>
 </alias>

 <alias>
  <family>sans-serif</family>
  <prefer>
   <family>IBM Plex Sans</family>
   <family>Noto Color Emoji</family>
  </prefer>
 </alias>
 -->

 <alias>
  <family>monospace</family>
  <prefer>
   <family>IBM Plex Mono</family>
   <family>Noto Color Emoji</family>
  </prefer>
 </alias>

 <alias>
  <family>courier</family>
  <prefer>
   <family>IBM Plex Mono</family>
   <family>Noto Color Emoji</family>
  </prefer>
 </alias>

 <!-- ════════════════════════
      FONT BLOCKING
      ════════════════════════ -->

 <!-- Symbola содержит чёрно-белые глифы для большинства
      Unicode-символов, включая эмодзи. Без блокировки он может
      перехватывать эмодзи-кодпоинты раньше Noto Color Emoji —
      в результате вместо цветных эмодзи отображаются
      монохромные символы. Если Symbola не установлен,
      этот блок безвреден. -->
 <selectfont>
  <rejectfont>
   <pattern>
    <patelt name="family">
     <string>Symbola</string>
    </patelt>
   </pattern>
  </rejectfont>
 </selectfont>

</fontconfig>
